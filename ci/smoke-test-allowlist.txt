# Regex allowlist for expected startup noise during the plugin smoke test.
# One regex per line.
# Blank lines and lines starting with # are ignored.
#
# Start with this file empty and add patterns only after investigating
# the corresponding log output.
